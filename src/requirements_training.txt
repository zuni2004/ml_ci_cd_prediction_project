# ════════════════════════════════════════════════════════════════════════════
# CI/CD BUILD FAILURE PREDICTOR - MODEL TRAINING REQUIREMENTS
# ════════════════════════════════════════════════════════════════════════════
#
# Install with: pip install -r requirements_training.txt
#
# These are the minimum required packages for training and inference.
# Optional packages are commented out.
#

# ── CORE DATA SCIENCE ─────────────────────────────────────────────────────
pandas>=1.5.0                          # Data manipulation and I/O
numpy>=1.23.0                          # Numerical computing
scikit-learn>=1.1.0                    # ML models, metrics, preprocessing

# ── HYPERPARAMETER OPTIMIZATION ───────────────────────────────────────────
optuna>=3.0.0                          # Bayesian hyperparameter search
                                       # (Tree-structured Parzen Estimator)

# ── JOB MANAGEMENT ────────────────────────────────────────────────────────
joblib>=1.2.0                          # Parallel processing, pickle

# ── OPTIONAL (for additional features) ────────────────────────────────────
# matplotlib>=3.5.0                    # Visualization
# seaborn>=0.12.0                      # Statistical visualization
# xgboost>=1.6.0                       # (Faster alternative to Random Forest)
# lightgbm>=3.3.0                      # (Even faster alternative)
# psutil>=5.9.0                        # System monitoring (RAM, CPU)

# ── API DEPLOYMENT (optional) ─────────────────────────────────────────────
# fastapi>=0.100.0                     # REST API framework
# uvicorn>=0.23.0                      # ASGI server
# pydantic>=2.0.0                      # Data validation
